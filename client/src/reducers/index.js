const initialState = {
    currentUser: {
        id: "", 
        username: ""
    }
}


export default (state = initialState, action) => {



    switch(action.type) {
        default: 
            return state; 
    }
}